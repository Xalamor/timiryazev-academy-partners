# Документация проекта Timiryazev Academy Partners

Проект — это Next.js-приложение, которое показывает международных партнёров Тимирязевской академии на интерактивной карте и в виде списков/страниц по странам.

## 📁 Структура проекта

### Корневые файлы
- **.gitignore** — файл для Git, перечисляет файлы/папки, которые не должны попадать в репозиторий
- **package.json** — описание npm-пакета с зависимостями и скриптами
- **tsconfig.json** — настройки TypeScript с алиасами путей
- **next.config.ts** — конфигурация Next.js
- **eslint.config.mjs** — конфигурация ESLint
- **postcss.config.mjs** — конфигурация PostCSS для Tailwind CSS
- **README.md** — документация проекта (этот файл)

### Основные скрипты package.json
```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
```
